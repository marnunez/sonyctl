[package]
name = "sonyctl"
version = "0.1.0"
edition = "2021"
description = "CLI control for Sony WH-1000XM series headphones over Bluetooth RFCOMM"
license = "MIT"
repository = "https://github.com/marnunez/sonyctl"

[dependencies]
clap = { version = "4", features = ["derive"] }
libc = "0.2"
